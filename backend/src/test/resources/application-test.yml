spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:wordgame-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

korean-dic:
  api-key: test-key

server:
  servlet:
    session:
      cookie:
        secure: false
